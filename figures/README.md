# Figures

Generated figures referenced by the website and report.

Commit the generating code alongside the image so every figure can be rebuilt.
Name files to match the question they answer, e.g. `rq1-class-distribution.png`.
