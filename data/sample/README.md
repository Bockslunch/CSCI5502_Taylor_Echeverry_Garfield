# Sample

A small, reproducibly generated excerpt of CICIDS2017 so the repository can be
inspected and code exercised without obtaining the full 1 GB dataset.

Generate with `src/sample.py`. Keep it to a few thousand rows.
